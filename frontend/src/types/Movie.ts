export type Movie = {
    id: string,
    movieName: string,
    imdbRating: number,
    headliner: string,
    releaseYear: number,
    posterPath: string,
    overview: string,
    themoviedbId: string,
    createdAt: Date,
    updatedAt: Date
}